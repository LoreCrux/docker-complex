services:
  client:
    image: "tylerdurham/multi-client:latest"
    mem_limit: 128m
    hostname: client
  server:
    image: "tylerdurham/multi-server:latest"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "tylerdurham/multi-worker:latest"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "tylerdurham/multi-nginx:latest"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
